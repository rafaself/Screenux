services:
  dev:
    build:
      context: .
      dockerfile: docker/Dockerfile.dev
    working_dir: /workspace
    volumes:
      - .:/workspace
    command: bash
    stdin_open: true
    tty: true
